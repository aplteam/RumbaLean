 OpenLogFile←{
     ⍝ ⍵ ←→ Server or Client
     ⍝ ← ←→ Tie Number
     f←⍵.Folder
     f≡'':0
     fixPath←{{'/'=¯1↑⍵:¯1↓⍵ ⋄ ⍵}'/'@('\'=⊢)⍵}
     f←fixPath f
     lf←{{CreateFolder ⍵}⍣(~⎕NEXISTS ⍵)⊣⍵}f  ⍝ Kai
     fn←lf,'/log.txt'
     22::fn ⎕NCREATE 0
     fn ⎕NTIE 0
 }
